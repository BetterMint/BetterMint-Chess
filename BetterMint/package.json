{
  "name": "bettermint",
  "version": "3.0.0",
  "private": true,
  "description": "BetterMint - universal chess assistant",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
